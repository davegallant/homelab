http:
  routers:
    suggestarr:
      rule: Host(`suggestarr.snake-cloud.ts.net`)
      entrypoints:
        - "https"
      service: suggestarr
      tls:
        certResolver: myresolver
  services:
    suggestarr:
      loadBalancer:
        servers:
          - url: "http://suggestarr:5000"
